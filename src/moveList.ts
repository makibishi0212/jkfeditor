import MoveCell from './model/move'
import Move from './model/move'

// 将棋用の指し手を管理するリストクラス

export default class MoveList {
  public currentMoves: Array<Move>

  constructor(moves: Array<Object>) {}
}
